{
  "name": "job-matcher-extension",
  "version": "1.0.0",
  "description": "Chrome extension for matching jobs to your resume",
  "private": true,
  "comment": "No runtime dependencies: the extension ships as plain scripts. PDF parsing uses the vendored lib/pdf.min.js; DOCX parsing is implemented directly in lib/parser.js. The former mammoth/pdf-parse dependencies and the esbuild build:parser script were never imported or invoked by any code path."
}
